{
    "name": "frostmark-gui",
    "license": "AGPL-3.0",
    "version": "0.0.3",
    "private": true,

    "dependencies": {
        // basic react deps
        "react": "^16.8.5",
        "react-dom": "^16.8.5",
        "react-scripts": "2.1.8",

        // comfortable API requests
        "node-fetch": "^2.3.0",

        // TreeView element
        "react-treeview": "^0.4.7",

        // Modal element
        "react-modal": "^3.8.1",

        // Font icons
        "font-awesome": "^4.7.0"
    },

    // deps used only for the running via Docker
    "devDependencies": {
        // proxy middleware for multiple endpoints with src/setupProxy.js
        "http-proxy-middleware": "^0.19.1"
    },

    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test",
        "eject": "react-scripts eject"
    },

    "eslintConfig": {
        "extends": "react-app"
    },

    "browserslist": [
        ">0.2%",
        "not dead",
        "not ie <= 11",
        "not op_mini all"
    ]
}
